To compile the files use the command： javac *.java

To execute use the command： java BlackJack -arg0 -arg1 -arg2 
arg0 is the maximum value of card. 
arg1 is the lower bound of winning range.
arg2 is the upper bound of winning range.

the command should be like: java BlackJack 3 10 15
